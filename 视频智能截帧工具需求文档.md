# 视频智能截帧工具需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 视频智能截帧工具 |
| 文档定位 | 当前成品版统一需求文档 |
| 适用版本 | 当前仓库实现版本 |
| 部署形态 | 局域网 / 单机 Web 应用 |
| 目标用户 | 需要从视频中快速提取可用截图的运营、设计、内容制作人员 |

## 2. 产品目标

本工具用于将单个视频快速处理为一组可筛选、可预览、可下载的关键截图，并支持在截图上叠加水印和 Logo，适合本地或局域网环境部署使用。

核心目标如下：

1. 降低人工拖动进度条逐帧截图的成本。
2. 通过自动过滤黑白屏、模糊帧、重复帧，提升截图可用率。
3. 提供可视化预览、单张下载和批量下载能力。
4. 支持在下载结果中写入水印和 Logo，满足对外分发和品牌展示需要。

## 3. 产品范围

当前版本覆盖以下业务范围：

1. 上传单个视频文件。
2. 配置截帧数量、质量、格式、分辨率。
3. 调用 FFmpeg 抽取候选帧并进行智能筛选。
4. 展示结果图片，支持选择、预览、单张下载、批量 ZIP 下载。
5. 上传水印和 Logo 素材，并在预览与下载中应用。
6. 自动清理运行过程中产生的历史文件。

当前版本不包含以下能力：

1. 多视频批量排队处理。
2. 用户账号、任务历史、数据库持久化。
3. 云端存储和远程文件管理。
4. Logo 九宫格定位或拖拽定位。
5. 按图片长期保存不同水印 / Logo 配置方案。

## 4. 用户流程

标准使用流程如下：

1. 上传一个视频文件。
2. 设置截帧参数。
3. 点击“开始截帧”。
4. 等待系统完成抽帧和筛选。
5. 在结果区预览并勾选截图。
6. 如需叠加效果，上传水印或 Logo 并调整参数。
7. 下载单张截图或批量下载 ZIP 文件。

## 5. 功能需求

### 5.1 视频上传

#### 5.1.1 上传方式

1. 支持点击选择文件上传。
2. 支持拖拽文件上传。
3. 单次仅处理一个视频文件。

#### 5.1.2 支持格式

支持以下视频格式：

- `mp4`
- `avi`
- `mov`
- `wmv`
- `flv`
- `mkv`

#### 5.1.3 限制规则

1. 默认最大上传大小为 300MB。
2. 后端限制通过 `MAX_FILE_SIZE` 配置控制。
3. 上传失败时需要返回明确错误提示。

#### 5.1.4 上传反馈

1. 显示上传进度。
2. 上传成功后显示视频基础信息。
3. 上传失败时提示文件格式不支持、文件过大或接口异常。

### 5.2 截帧参数设置

#### 5.2.1 截图数量

1. 支持设置截图数量。
2. 取值范围为 1 到 50。
3. 默认值为 10。

#### 5.2.2 图片质量

支持三档：

- `low`
- `medium`
- `high`

默认值为 `medium`。

#### 5.2.3 输出格式

支持两种输出格式：

- `jpg`
- `png`

默认值为 `jpg`。

#### 5.2.4 输出分辨率

支持三种分辨率策略：

- 原始分辨率
- `720p`
- `1080p`

默认值为原始分辨率。

#### 5.2.5 重置能力

支持清空当前视频、已生成截图、选择状态和效果设置，回到初始状态重新开始。

### 5.3 抽帧与智能筛选

#### 5.3.1 抽帧逻辑

1. 基于视频总时长均匀生成候选时间点。
2. 候选帧数量按目标截图数的 1.5 倍向上取整生成。
3. 通过 FFmpeg 在对应时间点截取静态帧。

#### 5.3.2 筛选逻辑

系统需要对候选帧执行以下筛选：

1. 过滤黑屏或白屏画面。
2. 过滤明显模糊画面。
3. 过滤重复或高度相似画面。
4. 尽量保留清晰度更高、内容更丰富的图片。

#### 5.3.3 进度反馈

1. 在抽帧阶段显示当前进度。
2. 在筛选阶段显示状态和完成度。
3. 前后端通过 Socket.IO 同步处理进度。

### 5.4 截图展示与预览

#### 5.4.1 结果展示

1. 使用网格方式展示筛选后的截图。
2. 每张图支持勾选、预览、下载。
3. 页面支持多图结果场景下的批量操作。

#### 5.4.2 预览能力

1. 点击图片可打开预览弹窗。
2. 预览弹窗支持查看当前图片。
3. 预览弹窗支持在结果集中切换上一张和下一张。
4. 预览画面需要实时叠加当前启用的水印和 Logo 效果。

### 5.5 图片下载

#### 5.5.1 单张下载

1. 每张截图支持单独下载。
2. 如果当前启用了水印或 Logo，则单张下载按当前预览效果输出。
3. 单张下载时允许使用“仅当前图片下载 / 预览”的效果配置。

#### 5.5.2 批量下载

1. 支持勾选若干截图后批量下载。
2. 批量下载结果为 ZIP 压缩包。
3. ZIP 文件名可由前端传入，并在后端进行安全清洗。
4. 批量下载仅应用“应用到所有图片”的水印 / Logo 设置。

#### 5.5.3 选择操作

1. 支持全选。
2. 支持取消全选。
3. 支持只下载已选择图片。

### 5.6 水印功能

#### 5.6.1 开关与素材

1. 支持独立启用或关闭水印。
2. 支持上传一张水印图片。
3. 支持格式为 `png` 或 `jpg/jpeg`。
4. 单张素材大小限制为 5MB。

#### 5.6.2 参数设置

支持以下参数：

1. 是否平铺到整张图片。
2. 透明度，范围 0 到 100。
3. 应用范围：
   - 仅当前图片下载 / 预览
   - 应用到所有图片

默认值如下：

1. 未启用。
2. 不平铺。
3. 透明度 50。
4. 应用到所有图片。

#### 5.6.3 生效规则

1. 前端预览通过 Canvas 实时叠加水印。
2. 单张下载按当前图片实时效果导出。
3. 批量下载时，由后端将水印永久写入图片。

### 5.7 Logo 功能

#### 5.7.1 开关与素材

1. 支持独立启用或关闭 Logo。
2. 支持上传一张 Logo 图片。
3. 支持格式为 `png` 或 `jpg/jpeg`。
4. 单张素材大小限制为 5MB。

#### 5.7.2 参数设置

支持以下参数：

1. 水平位置 `X`，范围 0 到 100。
2. 垂直位置 `Y`，范围 0 到 100。
3. 透明度，范围 0 到 100。
4. 尺寸比例，范围 5 到 30。
5. 应用范围：
   - 仅当前图片下载 / 预览
   - 应用到所有图片

默认值如下：

1. 未启用。
2. `X = 90`
3. `Y = 10`
4. 透明度 `80`
5. 尺寸 `10`
6. 应用到所有图片。

#### 5.7.3 生效规则

1. 前端预览通过 Canvas 实时叠加 Logo。
2. 单张下载按当前图片实时效果导出。
3. 批量下载时，由后端将 Logo 永久写入图片。

### 5.8 水印与 Logo 叠加规则

1. 水印和 Logo 可以同时启用。
2. 预览区域需要呈现二者叠加后的最终效果。
3. 批量下载时，后端需要按同样顺序将效果写入图片。
4. 预览效果与下载结果应保持一致。

### 5.9 运行期文件管理

系统运行过程中会产生以下目录内容：

1. `backend/uploads`
2. `backend/frames`
3. `backend/watermarks`
4. `backend/logos`

系统需要具备自动清理能力：

1. 上传视频默认保留 24 小时。
2. 抽帧结果默认保留 24 小时。
3. 水印和 Logo 素材默认保留 7 天。
4. 批量下载过程中的临时处理文件应在打包后清理。
5. 清理周期支持通过环境变量调整。

## 6. 非功能需求

### 6.1 部署要求

1. 支持 Windows 环境部署。
2. 支持通过发布包直接交付给其他电脑使用。
3. 支持优先使用发布包内置的 Node 安装包和 FFmpeg。

### 6.2 可维护性

1. FFmpeg 路径不可写死为开发者本机目录。
2. 运行参数需要通过环境变量或发布包目录结构控制。
3. 核心功能应具备基础自动化测试。

### 6.3 性能与体验

1. 需要在处理过程中持续给出进度反馈。
2. 页面在桌面端使用时应保持清晰、稳定、易操作。
3. 常见错误场景需要有明确提示，避免静默失败。

## 7. 技术实现约束

当前版本采用以下实现方式：

1. 前端：React + Vite + Ant Design。
2. 后端：Node.js + Express + Socket.IO。
3. 抽帧：FFmpeg / ffprobe。
4. 图片筛选：Jimp + 图像哈希。
5. 水印 / Logo 落盘：Sharp。
6. 批量打包：Archiver。

FFmpeg 查找顺序如下：

1. `FFMPEG_PATH` / `FFPROBE_PATH`
2. 项目内 `installers/ffmpeg/bin`
3. 系统 `PATH`

## 8. 验收标准

满足以下条件可视为当前版本验收通过：

1. 支持上传 300MB 以内的单个视频并完成抽帧。
2. 可根据设置输出指定数量的截图。
3. 系统能自动过滤明显黑白屏、模糊帧和重复帧。
4. 结果页支持预览、勾选、单张下载和批量 ZIP 下载。
5. 水印功能支持上传、预览、平铺、透明度调整和批量落盘。
6. Logo 功能支持上传、预览、X/Y 定位、透明度调整、尺寸调整和批量落盘。
7. 前端预览与实际下载结果在效果上保持一致。
8. 发布包在包含 Node 安装包和 FFmpeg 目录时，可在新电脑上离线完成环境准备并启动。
9. 运行过程中产生的过期文件可按策略清理。

## 9. 当前版本说明

为避免后续理解偏差，当前版本特别说明如下：

1. Logo 定位采用 `X/Y` 滑块，不是九宫格，也不是拖拽定位。
2. “仅当前图片下载 / 预览”表示只影响当前预览和单张下载，不会保存为长期的单图独立配置。
3. 批量下载只应用“应用到所有图片”的效果设置。
4. 项目当前定位是本地 / 局域网可用的成品工具，不是多用户 SaaS 系统。
