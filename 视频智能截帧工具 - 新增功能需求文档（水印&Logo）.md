# 视频智能截帧工具 - 新增功能需求文档（水印&Logo）
## 1. 文档基础信息
| 项目名称 | 视频智能截帧工具（水印&Logo功能扩展） |
|----------|----------------------------------------|
| 文档版本 | V1.1 |
| 适用范围 | 局域网网页版视频截图工具 |
| 新增目标 | 为截取的图片增加水印、Logo自定义添加功能，支持可视化预览和参数配置 |

## 2. 新增核心功能需求
### 2.1 图片水印添加功能
#### 2.1.1 功能开关与触发
- **功能勾选**：在截帧参数设置区增加「添加水印」勾选框，默认未勾选
- **触发方式**：勾选后，点击「添加水印」按钮触发水印添加流程；取消勾选则移除已添加的水印
- **操作反馈**：添加/移除水印过程有加载提示，完成后有文字+视觉反馈

#### 2.1.2 水印素材管理
- **上传功能**：支持上传自定义水印图片（支持PNG、JPG格式，建议透明底PNG）
- **素材预览**：上传后显示水印缩略图，支持删除已上传的水印素材
- **素材限制**：单张水印图片大小≤5MB，支持保存最近使用的3个水印素材（可选）

#### 2.1.3 水印参数设置
- **满屏设置**：提供「水印满屏」开关，勾选后水印图片自动平铺填充整个截图画面，取消则仅显示单张水印
- **透明度调节**：提供滑块调节水印透明度（范围0-100%，默认50%），实时预览调节效果
- **尺寸适配**：满屏模式下自动适配截图分辨率，保持水印比例不变形
- **应用范围**：水印参数设置后，可选择「应用到所有图片」或「仅当前图片」

#### 2.1.4 预览与生效
- **实时预览**：参数调整后，图片展示区的截图实时显示水印效果，无需重新生成
- **生效范围**：水印效果对下载的单张/批量图片均生效
- **重置功能**：提供「重置水印设置」按钮，恢复默认参数（透明度50%、非满屏）

### 2.2 图片Logo添加功能
#### 2.2.1 功能开关与触发
- **功能勾选**：在截帧参数设置区增加「添加Logo」勾选框，默认未勾选
- **触发方式**：勾选后，点击「添加Logo」按钮触发Logo添加流程；取消勾选则移除已添加的Logo
- **操作反馈**：添加/移除Logo过程有加载提示，完成后有文字+视觉反馈

#### 2.2.2 Logo素材管理
- **上传功能**：支持上传自定义Logo图片（支持PNG、JPG格式，建议透明底PNG）
- **素材预览**：上传后显示Logo缩略图，支持删除已上传的Logo素材
- **素材限制**：单张Logo图片大小≤5MB，支持保存最近使用的3个Logo素材（可选）

#### 2.2.3 Logo参数设置
- **位置设置**：提供8个固定位置选项（左上、中上、右上、左中、右中、左下、中下、右下）+ 自定义位置（拖动Logo预览图调整）
- **透明度调节**：提供滑块调节Logo透明度（范围0-100%，默认80%），实时预览调节效果
- **尺寸调节**：提供滑块调节Logo大小（按截图比例，范围5%-30%，默认10%），保持比例不变形
- **应用范围**：Logo参数设置后，可选择「应用到所有图片」或「仅当前图片」

#### 2.2.4 预览与生效
- **实时预览**：参数调整后，图片展示区的截图实时显示Logo效果，无需重新生成
- **生效范围**：Logo效果对下载的单张/批量图片均生效
- **重置功能**：提供「重置Logo设置」按钮，恢复默认参数（位置右上、透明度80%、尺寸10%）

### 2.3 界面交互适配
- **布局调整**：在原「截帧参数设置区」新增「水印设置」和「Logo设置」折叠面板，默认收起，勾选功能后展开
- **操作逻辑**：水印和Logo功能可同时开启，效果叠加显示，预览区可直观看到最终效果
- **批量应用**：支持一次性将当前水印/Logo设置应用到所有已截取的图片，也可单独对某张图片调整
- **效果保留**：下载图片时，水印和Logo效果会被永久保存到图片文件中，预览效果与下载效果一致

## 3. 非功能需求适配
### 3.1 性能要求
- 水印/Logo添加处理速度：单张图片添加效果≤0.5秒，批量10张图片≤3秒
- 参数调整实时预览响应：调整滑块后预览效果更新≤0.3秒
- 不影响原有截帧、下载功能的性能

### 3.2 兼容性要求
- 水印/Logo功能兼容所有支持的截图格式（JPG/PNG）
- 自定义位置调整功能兼容主流浏览器（Chrome/Firefox/Edge）

### 3.3 易用性要求
- 水印/Logo设置面板布局清晰，参数调节直观（滑块+预览）
- 提供默认水印/Logo示例效果，帮助用户快速理解功能
- 操作错误时（如未上传素材点击添加）有明确的提示文字

## 4. 技术实现建议
### 4.1 前端实现
- 水印/Logo预览：使用Canvas API在前端实时绘制效果，无需频繁请求后端
- 素材上传：与视频上传复用同一套上传逻辑，单独存储水印/Logo素材
- 参数存储：将水印/Logo参数（位置、透明度、尺寸）存储在前端状态中，应用时同步到后端

### 4.2 后端实现
- 图片处理：使用ImageMagick/OpenCV处理图片水印/Logo叠加，核心命令示例：
  ```bash
  # 添加水印（满屏+透明度50%）
  convert target.jpg -modulate 100,100,50 -tile watermark.png -draw "color 0,0 reset" result.jpg

  # 添加Logo（右上位置+透明度80%+尺寸10%）
  composite -dissolve 80% -gravity northeast -resize 10% logo.png target.jpg result.jpg
  ```
- 效果生效：下载图片时，后端根据前端传递的参数，将水印/Logo永久叠加到图片文件中
- 临时文件：水印/Logo素材临时存储，与截图文件同步清理

## 5. 验收标准（新增）
1. 水印/Logo素材上传正常，支持预览和删除，格式/大小限制生效
2. 水印满屏/透明度设置实时预览，效果符合预期，无变形
3. Logo位置/透明度/尺寸设置实时预览，自定义拖动位置精准
4. 水印和Logo可同时添加，效果叠加无冲突
5. 下载的图片中水印/Logo效果永久保存，与预览效果一致
6. 取消勾选后，水印/Logo可快速移除，恢复原图效果

### 总结
1. **核心新增功能**：为截图增加可配置的水印（支持满屏/透明度）和Logo（支持位置/透明度/尺寸）功能，均支持勾选开关、实时预览、单独/批量应用。
2. **交互核心**：所有参数调整实时预览，不依赖后端刷新，保证操作流畅性；水印和Logo可叠加使用，效果与最终下载文件一致。
3. **技术要点**：前端通过Canvas实现预览，后端通过ImageMagick/OpenCV完成图片永久叠加，素材与截图文件同步清理，保证性能和数据安全。
